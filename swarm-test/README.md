# swarm-test

A basic Hello World Node.js project to demonstrate imports and exports.

## Prerequisites

* Node.js (v14.x or later recommended)

## Usage

1. From the project root, run the application:

    ```bash
    npm start
    ```

    Alternatively, run directly:

    ```bash
    node app.js
    ```
